module.exports = {
    soma(n1, n2) {
        return (n1 + n2).toFixed(2)
    },

    subtracao(n1, n2) {
        return (n1 - n2).toFixed(2)
    },

    multiplicacao(n1, n2) {
        return (n1 * n2).toFixed(2)
    },

    divisao(n1, n2) {
        return (n1 / n2).toFixed(2)
    }
}